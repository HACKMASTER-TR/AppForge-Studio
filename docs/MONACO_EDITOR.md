# Monaco Editor

v1.3 installs `monaco-editor` as a local npm dependency.

The Build Service exposes its assets at:

`/vendor/monaco/`

Web Studio uses the locally served Monaco loader, so the editor does not depend on an external CDN.

Current editor:
- HTML syntax highlighting
- VS Dark theme
- automatic layout
- word wrapping
- live sandbox preview
- project save/open integration

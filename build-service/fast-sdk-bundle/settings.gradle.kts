rootProject.name = "appforge-fast-sdk-bundle"

dependencyResolutionManagement {
    repositories {
        maven {
            url = uri(
                "https://dl.google.com/dl/android/maven2/"
            )
        }

        mavenCentral()
    }
}
